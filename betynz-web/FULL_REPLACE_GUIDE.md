# Betynz v3.8.0 clean replacement

Use a blank repository. Upload the contents of this folder directly to the repository root. Do not place the folder itself inside another folder.

The root must contain `package.json`, `render.yaml`, `src`, `public`, `sql`, `test` and `scripts`.

The companion SportyBet API must be deployed first. Do not reuse any deleted Betynz Render, GitHub or Supabase credentials.
