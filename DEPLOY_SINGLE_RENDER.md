# Deploy Betynz v5.0.12 on one Render service

Use the root `render.yaml`. Keep `API_FOOTBALL_KEY` private in Render. Deploy with **Clear build cache & deploy**, then confirm `/api/health` reports version `5.0.12`.

No Supabase migration is required for this patch.
