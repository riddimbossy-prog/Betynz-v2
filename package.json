{
  "name": "betynz-api-football-only",
  "version": "5.0.10",
  "private": true,
  "type": "module",
  "description": "Single-Render Betynz platform using API-Football with five independent prediction engines and unified Betynz branding.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node apps/web/src/server.mjs",
    "build": "npm run clean:provider-reset && npm --prefix apps/web run build && npm run verify:single-render",
    "test": "npm run build",
    "test:integration": "node test/api-football-single-service-smoke.mjs",
    "verify:single-render": "node scripts/verify-single-render.mjs",
    "clean:provider-reset": "node scripts/clean-provider-reset.mjs"
  }
}
