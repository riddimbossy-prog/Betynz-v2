{
  "name": "betynz-unified-sportybet-primary-api-football-enrichment",
  "version": "4.0.3",
  "private": true,
  "type": "module",
  "description": "Single-Render Betynz platform: SportyBet custom API is primary for fixtures, odds, live, results and engine statistics; API-Football supplies crests and secondary enrichment only.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/start-combined.mjs",
    "build": "npm run build:api && npm run build:web && npm run verify:single-render",
    "build:api": "npm --prefix apps/sportybet-api run build",
    "build:web": "npm --prefix apps/web run build",
    "test": "npm run build",
    "test:integration": "node test/combined-smoke.mjs",
    "verify:single-render": "node scripts/verify-single-render.mjs"
  }
}
