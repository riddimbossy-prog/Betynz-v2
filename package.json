{
  "name": "betynz-api-football-only",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "description": "Single-Render Betynz platform using API-Football as the sole football data provider for fixtures, odds, live scores, results, statistics and visuals.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node apps/web/src/server.mjs",
    "build": "npm --prefix apps/web run build && npm run verify:single-render",
    "test": "npm run build",
    "test:integration": "node test/api-football-single-service-smoke.mjs",
    "verify:single-render": "node scripts/verify-single-render.mjs"
  }
}
